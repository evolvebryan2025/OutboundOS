[build]
builder = "DOCKERFILE"
dockerfilePath = "worker/Dockerfile"
watchPatterns = ["worker/**"]

[deploy]
startCommand = "cd /app && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
